spring:
  application:
    name: clinic-service
  datasource:
    .url:
      jdbc:postgresql://localhost:5432/clinic_service
    .username: postgres
    .password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


